/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `person_age_weight`;

DROP TABLE IF EXISTS `person_emails`;

DROP TABLE IF EXISTS `person_nicknames`;

DROP TABLE IF EXISTS `person`;

CREATE TABLE `person` (
  `id` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `first` TEXT NOT NULL,
  `last` TEXT NOT NULL)
 ENGINE=InnoDB;

CREATE TABLE `person_nicknames` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `index` BIGINT UNSIGNED NOT NULL,
  `value` TEXT NOT NULL,
  CONSTRAINT `person_nicknames_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `person` (`id`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `person_nicknames` (`object_id`);

CREATE INDEX `index_i`
  ON `person_nicknames` (`index`);

CREATE TABLE `person_emails` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `value` TEXT NOT NULL,
  CONSTRAINT `person_emails_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `person` (`id`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `person_emails` (`object_id`);

CREATE TABLE `person_age_weight` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `key` SMALLINT UNSIGNED NOT NULL,
  `value` FLOAT NOT NULL,
  CONSTRAINT `person_age_weight_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `person` (`id`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `person_age_weight` (`object_id`);

